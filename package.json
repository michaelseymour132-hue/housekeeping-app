{
  "name": "housekeeping-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Server dependencies for the Vercel serverless functions in /api. The front end is plain static HTML/CSS/JS and needs none of this.",
  "dependencies": {
    "firebase-admin": "^12.7.0"
  }
}
